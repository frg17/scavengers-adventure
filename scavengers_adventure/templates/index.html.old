{% extends 'base.html' %} {% block content %}
<div>
  <p>This is supposed to be much prettier, but who am I? A stylist?</p>
  <p>This is also supposed to be more of a proof of concept.</p>
</div>

<div class="form-container">
{% with messages = get_flashed_messages()%} {% if messages %}
<ul class="flashes">
  {% for message in messages %}
  <li>{{ message }}</li>
  {% endfor %}
</ul>
{% endif %} {% endwith %}
<label>Decrypt a supersecret file here:</label>
<form
  id="theform"
  action="/decrypt"
  method="post"
  enctype="multipart/form-data"
>
  <input class="inp" type="text" name="secret-key" placeholder="Supersecret key here."/><br />
  <input class="inp" type="file" name="file-input" /><br />
  <input type="submit" value="Decrypt"/>
</form>

<a class="supersecret-image" href="/static/supersecret.jpg" download>Supersecret encrypted file.</a>

</div>


<script>
  const form = document.getElementById("theform");
  const form2 = form.cloneNode(true);
  form2.id = "theform2";
  form.addEventListener("submit", () => {
    setTimeout(() => {
      let inputs = document.querySelectorAll(".inp");
      for (let i = 0; i < inputs.length; i++) {
        inputs[i].value = "";
      }
    }, 1000);
  });
</script>
{% endblock %}
